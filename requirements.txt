# Core: code/qwen_img_edit.py, scripts/run_qwen_img_edit.py
# CUDA 12.4 wheels; other setups: https://pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.11.0
vllm==0.20.1
transformers>=4.50.3,<6.0.0
# QwenImageEditPlusPipeline and related pipelines need a current diffusers; raise floor if ImportError
diffusers>=0.31.0
transformers>=4.50.3,<5.0.0
accelerate>=0.26.0
Pillow>=10.0.0
requests>=2.28.0
qwen-vl-utils
# Video for scripts/run_qwen_vl.py --video: qwen_vl_utils tries torchvision first; newer torchvision
# removed read_video. Install decord (or PyAV: pip install av) so video decoding works.
decord>=0.6.0

# Tests
pytest>=8.0

# Optional: vendored models/qwen_image/src/examples (Gradio demos, DashScope prompt utils) —
# pip install gradio spaces dashscope
numpy>=1.25.0

# Vendored ComfyUI server (comfyui/); run from that directory per upstream.
-r comfyui/requirements.txt
